name: Sign IPAs

on:
  workflow_dispatch:

jobs:
  sign:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run signing script
        run: |
          chmod +x sign_with_all_certs.sh
          ./sign_with_all_certs.sh